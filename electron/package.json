{
  "name": "@sql-ide/electron",
  "version": "1.0.0",
  "description": "Electron main process for SQL IDE",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && wait-on http://localhost:5173 && electron dist/main.js",
    "build": "tsc",
    "start": "electron dist/main.js"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "^28.1.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "wait-on": "^7.2.0"
  }
}
